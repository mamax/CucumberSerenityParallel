####UI Test automation Framework CucumberWithSerenity####

###How to run with Report Portal:###
1. Run with `mvn clean install`.
2. Run with `ScenarioReporter`, but you can also run with `StepReporter`.